language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t Yair-lahad/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true Yair-lahad/docker-react npm run test
